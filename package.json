{
  "name": "downtune",
  "version": "1.1.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "install-all": "npm install --prefix client && npm install --prefix server",
    "dev": "npx concurrently --names \"client,server\" --prefix-colors \"cyan,green\" \"npm run dev --prefix client\" \"NODE_ENV=development npm run start --prefix server\""
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
